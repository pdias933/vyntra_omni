# Dependências — PR 022

Nenhuma dependência foi adicionada.

O cadastro de `ContaWhatsApp` usa apenas NestJS, Prisma e PostgreSQL já aprovados. Não foi introduzido SDK Meta, cliente HTTP, biblioteca de cofre ou suporte a credencial. A integração real continua reservada às PRs de caracterização e do `AdaptadorMetaCloud`.
