# Dependências — PR 019

Nenhuma dependência foi adicionada.

O simulador usa `node:crypto` apenas para assinatura/determinismo de fixtures. Não foi introduzido SDK Meta, cliente HTTP, broker, storage em memória de produção ou biblioteca de mock. A futura seleção de SDK/HTTP pertence à caracterização do adapter real.
