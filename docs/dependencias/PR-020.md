# Dependências — PR 020

Nenhuma dependência foi adicionada.

O simulador usa `node:crypto` para assinatura e protocolo determinísticos. Não foi introduzido SDK MK, cliente HTTP, fila, banco em memória ou biblioteca de mock. A escolha de transporte e eventual SDK pertence à caracterização real do `AdaptadorMkSolutions`.
