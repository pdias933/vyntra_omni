# Dependências — PR 016

Nenhuma dependência foi adicionada.

O limite e a revogação reutilizam Prisma/PostgreSQL, autenticação, autorização e auditoria centrais. O advisory lock recebe somente uma chave interna derivada do UUID do usuário e protege uma seção transacional curta; Redis não participa da contagem ou decisão.

O app reutiliza SecureStore e o gerenciador de sessão da PR 015 para limpar a credencial após `401`. Nenhum pacote de WebSocket/sincronização foi antecipado, pois esses transportes ainda não existem.
