# PR 084 — editor visual

## Implantação

- não há migration nova; a marca permanece `20260901013500_corrida_resgate_envio_automatico`;
- implantar API e web do mesmo commit, com OpenAPI e SDK gerado já verificados;
- manter API e workers homogêneos; o editor não registra provider no runtime;
- validar prontidão `PRONTO` antes de expor a rota administrativa.

## Aceite

1. Com sessão web autorizada, listar e abrir fluxo/versão.
2. Criar rascunho, mover/configurar nós e salvar com revisão esperada.
3. Confirmar que o ponteiro de produção não mudou e que uma revisão concorrente falha.
4. Validar grafo inválido e confirmar permanência em `RASCUNHO`.
5. Validar grafo válido, confirmar `EM_TESTE` e publicar em comando separado.
6. Confirmar que nova execução usa o novo ponteiro e execução anterior conserva a versão original.
7. Confirmar ausência de definição, parâmetros, variável sensível ou credencial em logs/auditoria.

## Reversão operacional

Reverter web/API para a imagem anterior não exige alteração do banco. Versões criadas permanecem preservadas. Não alterar `estado`, `revisao` ou `versao_publicada_id` por SQL; use os serviços de domínio quando a interface administrativa estiver disponível.
