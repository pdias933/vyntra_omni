# PR 080 — nós de protocolo e ordem de serviço

## Implantação

1. Confirmar `20260901012500_espera_resposta_fluxo` como última migration; esta PR não cria migration.
2. Implantar API e todas as instâncias de `worker_fluxos` com a imagem PR 080.
3. Confirmar `/api/v1/saude/pronto` como `PRONTO` e imagens homogêneas.
4. Manter os nós indisponíveis onde o adapter ERP real não estiver registrado e caracterizado.

## Aceite

- executar `CRIAR_ATENDIMENTO` sem provider e confirmar `INDISPONIVEL`, sem nova operação ou protocolo;
- confirmar que protocolo oficial existente percorre `CRIADO` sem chamar o adapter;
- testar criação/reconciliação determinística e uma única operação por execução+nó;
- executar OS somente com confirmação explícita, contexto automatizável, contrato, fila e protocolo exatos;
- comprovar auditoria `FLUXO` sem usuário/sessão e sem assunto, descrição ou IDs externos;
- mudar a autoridade antes da confirmação e comprovar negação sem materializar OS;
- confirmar que a chamada externa ocorre fora da transação do executor.

## Recuperação

Não editar protocolo, OS, operação recuperável, passo ou execução por SQL. Resultado incerto deve aguardar a reconciliação do serviço de domínio; indisponibilidade anterior ao efeito segue a saída nominal. Rollback preserva operações, protocolos, OS e auditoria. Antes de voltar ao worker PR 079, interromper novas execuções e comprovar que nenhuma execução não terminal depende dos nós da PR 080.

## Resultado em staging

Preencher somente após o aceite real da imagem PR 080.
